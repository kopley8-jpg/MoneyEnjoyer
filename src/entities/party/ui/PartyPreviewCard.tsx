import { PartyType } from '@/shared/types/transaction';
import { StyleSheet } from 'react-native';

type PartyPreviewCardProps = {
  party: PartyType;
};

export const PartyPreviewCard = ({ party }: PartyPreviewCardProps) => {
  return <></>;
};

const useStyles = () => {
  return StyleSheet.create({});
};
